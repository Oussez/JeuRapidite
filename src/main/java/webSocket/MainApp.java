package webSocket;

public class MainApp {

    public static void main(String[] args) {

        // Assuming Game has setters or ways to inject these mocks
       // userStatusWebSocket.set
        //game.setSessions(MockData.createMockSessions());

        // Call beginGame with test data
       // game.beginGame("sender", 5, 2);
        //userStatusWebSocket.beginGame("salah",3,3);
    }
}
